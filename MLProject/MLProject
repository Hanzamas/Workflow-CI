name: rice-classification

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: string, default: "data_split"}
      epochs:    {type: float,  default: 2}
    command: "python modelling.py --data_path {data_path} --epochs {epochs}"
